// abstract classes in typescript :

// abstract classes are declared using the 'abstract' keyword.
// They cannot be instantiated directly, but can be inherited by other classes.
// Abstract classes can contain abstract methods, which must be implemented in the derived classes.
// Abstract classes can also contain non-abstract methods.

// abstract classes in typescript example :

// Imp : need to study this.
